{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Редактирование города</h1>

    {{ form(edit_form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('city') }}">
            Вернуться к списку
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
